language: cpp
compiler:
  - clang

general:
  branches:
    only:
      - master
      - new_lib

before_script:
  - mkdir build
  - cd build

script:
  - cmake build ..
  - make -j Tests
  - cd Tests/
  - ./Test

notifications:
  email: true

