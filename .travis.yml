language: C++

general:
  branches:
    only:
      - travis

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - libboost-all-dev

before_install:
  - git clone https://github.com/google/googletest
  - cd googletest
  - mkdir build
  - cd build
  - cmake -D CMAKE_CXX_FLAGS=-std=c++17 ..
  - make
  - cd ..
  - cd ..
  - git clone https://github.com/opencv/opencv.git
  - cd opencv
  - mkdir build
  - cd build
  - cmake cmake -D CMAKE_BUILD_TYPE=RELEASE -D BUILD_EXAMPLES=OFF -D BUILD_opencv_apps=OFF -D BUILD_DOCS=OFF -D BUILD_PERF_TESTS=OFF -D BUILD_TESTS=OFF build ..
  - make -j2
  - cd ..
  - cd ..



before_script:
  - mkdir build
  - cd build

script:
  - cmake build -DOpenCV_DIR=./opencv/build -DGtest_DIR=./googletest/build ..
  - make -j2
  - cd Tests/
  - ./Test

notifications:
  email: true

